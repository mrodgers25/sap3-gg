<%= form_for(@story_category) do |f| %>
  <% if @story_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story_category.errors.count, "error") %> prohibited this story_category from being saved:</h2>

      <ul>
      <% @story_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
