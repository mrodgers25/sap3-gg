<%= form_for(@media_info) do |f| %>
  <% if @media_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@media_info.errors.count, "error") %> prohibited this media_info from being saved:</h2>

      <ul>
      <% @media_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :media_type %><br>
    <%= f.text_field :media_type %>
  </div>
  <div class="field">
    <%= f.label :url_id %><br>
    <%= f.text_field :url_id %>
  </div>
  <div class="field">
    <%= f.label :media_desc %><br>
    <%= f.text_field :media_desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
