<div class="container">
  <%= form_for(@admin_setting, url: admin_admin_setting_path(@admin_setting), method: :patch) do |f| %>
    <div class="jumbotron bg-white admin-form">
      <div class="header text-center">
        <h4><i class="fas fa-users-cog mr-1"></i> Admin Settings</h4>
      </div>

      <hr class="my-4">

      <div class="body">
        <div class="form-row">
        <div class="col">
          <%= f.label :newsfeed_display_limit %><br>
          <%= f.number_field :newsfeed_display_limit, class: "form-control form-control-sm" %>
          <small class="form-text text-muted">The amount of stories displayed on the Newsfeed</small>
        </div>

        <div class="col">
          <%= f.label :filtered_display_limit %><br>
          <%= f.number_field :filtered_display_limit, class: "form-control form-control-sm" %>
          <small class="form-text text-muted">The amount of stories displayed on the page after a user applies filters</small>
        </div>

        <div class="col">
          <%= f.label :newsfeed_daily_post_count %><br>
          <%= f.number_field :newsfeed_daily_post_count, class: "form-control form-control-sm" %>
          <small class="form-text text-muted">The amount of stories that are posted each day from the Newsfeed Queue. ie: n = 10; (24 hrs / n) => 1 every 2.4 hours</small>
        </div>
      </div>

        <hr class="my-4">

        <div class="text-center">
          <%= f.submit "Save", class: 'btn btn-md btn-primary w-25' %>
        </div>
      </div>
    </div>
  <% end %>
</div>
