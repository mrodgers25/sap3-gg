<div class="container-fluid">
  <div class="jumbotron bg-white admin-stories admin-index-page">
    <div class="header text-center">
      <h4><i class="fas fa-sort-numeric-down mr-1"></i> Newsfeed Queue</h4>
    </div>

    <hr class="my-4">

    <%= render partial: 'filters', locals: { filter_path: admin_queued_items_path } %>

    <hr class="my-4">

    <div class="table-div">
      <table class="table table-bordered table-hover">
        <thead>
          <tr>
            <th scope="col">Title</th>
            <th scope="col">Queue Position</th>
            <th scope="col">Display Position</th>
            <th scope="col">Pinned?</th>
            <th scope="col">Unpublish At</th>
            <th scope="col">Published Count</th>
            <th scope="col">Displaying Date</th>
            <th scope="col">Actions</th>
          </tr>
        </thead>
        <tbody>
          <% publish_day_count = 1 %>
          <% @queued_items.each_with_index do |queued_item, index| %>
            <tr>
              <td style="width: 600px;"><%= queued_item.publishable.latest_url&.url_title %></td>
              <td><%= queued_item.queue_position || '-' %></td>
              <td><%= queued_item.position || '-' %></td>
              <td><%= human_bool(queued_item.pinned) %></td>
              <td><%= queued_item.unpublish_at&.strftime("%m/%d/%Y -  %I:%M %p")  || '-' %></td>
              <td><%= queued_item.publishable.published_count %></td>
              <td><%= (Time.zone.now + publish_day_count.day).beginning_of_day.strftime("%m/%d/%Y") %></td>
              <td style="width: 100px;">
                <%= link_to edit_admin_queued_item_path(queued_item), class: "btn btn-sm btn-outline-secondary m-1 w-100" do %>
                  <i class="fas fa-edit mr-1"></i> Edit
                <% end %>
                <%= button_to remove_admin_queued_item_path(queued_item), class: "btn btn-sm btn-outline-danger m-1 w-100", data: { confirm: 'Are you sure?' } do %>
                  <i class="fas fa-times mr-1"></i> Remove
                <% end %>
              </td>
            </tr>

            <% publish_day_count += 1 if (index % PublishedItem.publish_rate) == (PublishedItem.publish_rate - 1) %>
          <% end %>
        </tbody>
      </table>
    </div>

    <hr class="my-4">

    <div class='pagination'>
      <%== pagy_bootstrap_nav(@pagy) %>
    </div>
  </div>
</div>
