<div class="container">
  <%= form_for(@published_item, url: admin_published_item_path(@published_item)) do |f| %>
    <div class="jumbotron bg-white admin-form">
      <div class="header text-center">
        <h4><i class="fas fa-sort-numeric-down mr-1"></i> Edit Position</h4>
      </div>

      <hr class="my-4">

      <div class="body">
        <div class="form-group my-4 max-vw-80 mx-auto">
          <%= f.label :position %><br>
          <%= f.number_field :position, class: "form-control" %>
          <%= f.hidden_field :old_position, value: @published_item.position %>
        </div>

        <div class="form-group my-4 max-vw-80 mx-auto">
          <%= f.label :publish_at %><br>
          <%= f.datetime_field :publish_at, required: true, min: Date.today, class: "form-control" %>
        </div>

        <div class="form-group my-4 max-vw-80 mx-auto">
          <%= f.label :unpublish_at %><br>
          <%= f.datetime_field :unpublish_at, min: Date.today, class: "form-control" %>
        </div>

        <hr class="my-4">

        <div class="actions">
          <%= f.submit "Save", class: 'btn btn-md btn-primary text-center w-100' %>
        </div>
      </div>
    </div>
  <% end %>
</div>
